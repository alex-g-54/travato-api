<p>Find me in app/views/itineraries/show.html.erb</p>

<h1><%= @itinerary.name %></h1>

<%= form_tag("/itinerary_users", method: "post") do  %>
  <%= hidden_field_tag(:itinerary_id, @itinerary.id) %>
  <%= submit_tag("Book a spot on this tour") %>
<% end %>

<p><%= @itinerary.description %></p>

People coming: <%= @itinerary.spots_sold %> (Total seats left: <%= @itinerary.total_capacity - @itinerary.spots_sold %> of <%= @itinerary.total_capacity %>)
<ul>
  <% @itinerary.guests.each do |guest| %>
  <li><%= guest.full_name %></li>
  <% end %>
</ul>
